# ManagedCode.Tps Swift SDK

- Keep the Swift runtime behavior aligned with `SDK/ts`, `SDK/js`, `SDK/dotnet`, and `SDK/flutter`.
- Validate against `SDK/fixtures/**` and `examples/*.tps`.
- Public APIs must stay embeddable and UI-framework-neutral.
- Prefer immutable value models and deterministic playback math.
